# Changelog

## Unreleased

- Wired saved defaults into new web app creation for both GUI and CLI flows.
- Added backend validation for required name/category fields.
- Made generated desktop launchers safer and more portable, including AppImage-aware executable resolution.
- Added autostart entry management and background-on-close behavior for tray-enabled setups.
- Added JSON backup export/import with icons and settings included.
- Added managed path actions in the settings drawer.
- Expanded test coverage and CI checks with TypeScript, `cargo fmt`, and `cargo clippy`.
